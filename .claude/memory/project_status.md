# プロジェクト現在地

**最終更新: 2026/07/31**（このファイルは「現在地」のみ。履歴は書かない）

> 履歴・意思決定の経緯 → `design/V2/90_決定ログ.md`（SoT）
> 2026/07/27 以前の旧 project_status 全文 → `.claude/memory/archive/project_status_full_2026-07-27.md`

---

## 稼働中

| 系統 | 状態 |
|---|---|
| crawl.yml（毎日16:30 JST） | ✅ 稼働。実行 1h20m〜2h45m。直近8回で fail 1（7/25・修正済 7f98d032e） |
| C層分析 | combo_deep_dive.py / Phase C composite_hit_rate / ALL_FISH 59種 |
| D層予測 | forecast ページは **検証済みモデル**（前日CI の forecast_daily.json × open_tier tier A）を配信中 |
| 釣果価値チェッカー | ✅ リリース済（月報 wholesale.yml 月次 + 日報 fish-value-daily.yml 週次） |
| x_post 日次まとめ | ✅ 数値根拠のみの本文（insights.py / narrative.py） |

## 正直なKPI（BL-1リーク解消後・T45 で確定した値）

- cnt_avg wMAPE P50 = **47.1%**（公表していた 38.1% の約9pt はリークだった）
- **公表KPIは cnt_bz（ボウズ込み）が正**: レンジ的中率 P50 = **90.0%**
- open_tier **tier A = 109コンボ / 23魚種 / 62船宿**（pb≤10% & n≥50 & fallback除外）
- H=28 で model 47.6% vs BL2 51.0%・ペア勝率 69.1% ＝ 主戦場（1〜4週先）の優位は本物

## 出荷済み（詳細は決定ログの各日付）

T44/T44b（因子供給率 33.6→79.6%）/ T45（リーク解消・正直化）/ T47a・T47b（選別公開・
ペイウォール完全撤去）/ Tier2 fish_area 編集部ノート パイロット5 / x_post 本文刷新 /
釣果価値チェッカー / 不変条件 #53〜#56

---

## E-E-A-T（AdSense 対応・2026/07/27）

第三者 AdSense 診断で NG は C（E-E-A-T）の3項目のみ。A/B/D は全 OK、
「自動生成の疑い」も判定保留。監査対象は薄ページではなく分析セクション入りの上位ページ
だったため、**noindex 676本の削除では判定は動かない**（初期仮説を棄却）。

| 項目 | 対応 |
|---|---|
| C4 コンテンツ品質 | ✅ 旬ピークの母数下限＋定義併記＋海況実勢を追加（不変条件 #57） |
| C1 Experience | ✅ 運営者の一次体験 **19魚種・13,500字** + 共通FAQ 4問（不変条件 #58） |
| C2 Expertise | 一部。fish_area_notes はパイロット5本のまま |

- 一次体験は `normalize/field_reports.json`（書き方は `normalize/FIELD_REPORTS_GUIDE.md`）
- 体感をサイトの実データで裏取りできたものが10件（イサキの夏偏り・タチウオのテンヤ大型・
  イカ3種の混じり方・サバの外道率・アマダイの外道同定=ムシガレイ・出世魚の段階 等）。
  **この「体感→データ照合」の組み合わせが、自動収集にも体験ブログにも単独では書けない独自性**
- 港・船宿は「事実（駐車場・受付時間＝記憶で書かない）／体験（その日どうだったか＝書ける）」
  で分離するルールをガイドに明記。公式サイト391件の自動クロールは抽出精度が出ないため不採用

**残**: 港・船宿の体験（fish_area 近重複135本に最も効く・未着手）／C2 の fish_area_notes 拡大／
魚種は19種で一旦終了（残りは未釣行）

---

## インデックス状況（GSC 実測・2026/07/31 に直接確認）

**登録済み 779 / 未登録 922**（データは 2026-07-24 時点）

| 未登録の理由 | 件数 | 判定 |
|---|---:|---|
| noindex で除外 | 485 | 意図通り |
| 見つかりませんでした（404） | 287 | T49 で新規発生を停止 |
| 検出 - インデックス未登録 | 122 | 手動投入中（下記） |
| クロール済み - インデックス未登録 | **23** | ← ここが少ない |

**「クロール済み - 未登録」が 23件しかない = Google は中身を見て弾いてはいない。**
「薄いから弾かれている」は少なくとも現時点の主要因ではない（AdSense 判定とは別軸）。

- 検出-未登録 122件は**全件が「前回のクロール: 該当なし」＝一度もクロールされていない**。
  118件は実在・indexable・sitemap 掲載済み ＝ 純粋にクロールバジェットが回っていない
- 真因は fish_area の 404 フラッピング（2,141本中 **573本(26.8%)** が削除→復活）。
  → **T49 で孤児を削除せず noindex 墓標化＋大量削除ガード（不変条件 #59）** を投入して停止
- 手動インデックス登録の進捗・残リストは `analytics/gsc/manual_index_requests.md`（SoT）。
  2026-07-31 に 15本投入済み・残 102本。**1日約10〜12本が上限・ハブ優先**
- プロパティは `sc-domain:` ではなく **URL プレフィックス型** `https://funatsuri-yoso.com/`

**手動投入は効いている（2026-08-17 の効果測定）**: 投入18本の GSC 表示初出日を照合すると、
投入前の表示は全18本とも0行で、**10本が投入日当日〜3日後に初出**（omaezaki 73表示 / magochi 49 /
calendar 38 …）。未投入は機械検証して **70本**に確定し Day1〜7 の投入プランを
`analytics/gsc/manual_index_requests.md` に記載済み。**次の1本目は `area/urayasu`**。

⚠ GSC API は検索パフォーマンスのみ。「表示が出た＝インデックス済み」は言えるが逆は言えない。
**122件 / 404 287件の推移は管理画面（ページレポート・URL検査）でしか見られない = 要ブラウザ作業**。
「クロール済み - 未登録」が急増したら品質側の問題なので E-E-A-T 施策に戻る。

**title 文言の実験は打ち切り（2026-08-17）**: 8/5・8/6 の2施策とも CTR に効果が出ず、
表示量 約226/日では area CTR の1pt差を検出するのに数か月かかる＝逐次A/Bが成立しない。
3つ目を重ねると2件が永久に交絡するので、title はこれ以上いじらない。飯岡の主表記
入れ替え（港→漁港）も、全期間で取り直したら根拠が消えたので見送り。詳細は決定ログ 2026-08-17。

---

## ⚠ 停止中のパイプライン

1. ~~`weather/*.csv` が 2026-04-13 で停止~~ → **✅ 2026/08/01 復旧完了**。
   2026-04〜07 を再生成、日次追記を `ocean/update_weather_csv.py` として crawl.yml に復活、
   不変条件 #60（鮮度7日）で監視。代表名は `normalize/weather_point_map.json` にピン留め
   （選挙ドリフトで浦安沖→江戸川店前に化けて履歴分断していた）。決定ログ 2026-08-01
2. ~~**`direct-crawl` が空振り**（2026/07/27 発見）~~ → **✅ 2026/08/02 復旧完了**。
   真因は3つ重なっていた: ①gyo_crawler.py がどの workflow にも書かれていない
   ②crawler.py の CSV マージが `crawl/direct-crawl/...`（存在しないパス）を見ていて一度も
   実行されていなかった ③パーサーが history URL の日付を貼るせいで 63% が水増し。
   対象を**一之瀬丸のみ**に縮小（忠彦丸=chowari / 米元・勇幸丸=釣りビジョンでカバー済み）、
   parse_ichinose() 新設、direct-crawl.yml に組込、不変条件 #63 で監視。決定ログ 2026-08-02
   - **CSV 反映は手動 `--export-csv` 時**（shojiro / koueimaru と同じ運用）
   - 一之瀬丸は ships.json に無く ship ページが生成されない。登録すると fish_area/ship の
     母集団が変わる（T49 と同じ領域）ので影響を見てから別途判断
   - 過去分 2026-04-26〜07-16 は復旧不可（gyo は各便の最新釣果しか出さない）

---

## 次の候補（未着手・優先度順）

0. **手動インデックス投入 Day1〜7**（要ブラウザ）— `analytics/gsc/manual_index_requests.md`
   の投入プラン70本を上から消化。次は `area/urayasu`。ship 9本の noindex は動的なので投入直前に再検証
0b. **thin テンプレ本文刷新の効果測定** — 2026-08-17 出荷（不変条件 #66）。
   Google の再クロール後にしか出ないので **最短2週間は判定しない**。対象 thin 27本のうち
   直近28日に表示があるのは6本（amatsu 291・futtsu 143・enoura 38・fukuura-yugawara 34 ほか）。
   full 並みの CTR（2.28%）に届けば月+8クリック相当
1. **Tier2 拡大** — パイロット5 の GSC 反応を見て、残り約29ページへ同方式を展開
2. **fish_area 薄ページの物理削除/統合の検討** — 現在 noindex 止まり（676本）。
   noindex は巡回される。母集団を変えるなら削除が要る。※AdSense 判定は外から検証不可＝仮説
3. **T46** — Hurdle + log1p + recency加重（アブレーション → 全再実行1回に同梱）
4. **T48** — プール学習PoC（⚠ C層への外部ライブラリ解禁の可否＝**要ユーザー判断**）

## ブロック中 / 未着手

- AdSense 4連敗（「有用性の低いコンテンツ」）
- 決済（Stripe等）未着手 — マネタイズ方針は「予測は当面無料・月間数千〜1万UU後に再検討」
- crawl.yml Node.js 20→24

## 未マージブランチ

- `claude/unregistered-status-issue-149a5d`（3 commits・7/18）— GSC 診断ドキュメント中心。
  コード差分は fish_area noindex 閾値 80→50 の段階開放。**取り込むか破棄か判断が要る**
- 3月〜5月の残骸5本（confident-bose / sleepy-goldstine / pr-38 / pr-39 / redesign-update）は掃除対象

---

## 恒久ルール（破ると事故る）

- ローカル `crawler.py` フル実行の前に **必ず `git pull`**（未実施だと HERO 日付が巻き戻る）
  → PreToolUse フックで強制済み
- `crawl/validate_output.py` errors=0 を通してから push。**閾値を緩めて黙らせるのは禁止**
- 全再実行後は `build_predict_params.py` と `build_open_tier.py` の**両方**をローカル実行してコミット
- 分析実行はメイン repo（worktree 不可）
