# Phase C composite_hit_rate 実装計画（plan_C_2026-05-08.md）v1.2

作成: pm（v1）/ ユーザー判断4点反映（v1.1）/ 4並列レビュー CRITICAL3+MAJOR3+MINOR3 対応（v1.2）
ステータス: v1.2 修正完了・4並列再レビュー待ち
前提: Phase B-α'（size 改善・補遺8 採用確定）/ Phase B-β-4（kg 改善・補遺9 採用確定）完了

---

## §1 背景・目的

### 1.1 Phase A/B 完了状況

`90_決定ログ.md` 補遺8/9 確定事項:

| metric | n（H=0, n>=30）| promise_break P50 | 改善幅 | 状態 |
|---|---|---|---|---|
| cnt | 290 | 6.57% | （元々良好）| Phase A 完了 |
| size | 181 | 31.25% | +67.2pt（同一定義）| Phase B-α' 採用確定（補遺8）|
| kg | 103 | 23.93% | +31.63pt | Phase B-β-4 採用確定（補遺9）|

3 メトリックの個別改善が揃ったことで、これらを統合した composite 集計（Phase C）に進む段階に達した。

### 1.2 本 Plan の位置づけ

`plan_hit_rate_2026-05-08.md` §C（C-1〜C-4）が Phase C の骨格を定義している。本 Plan は Phase C 単体の実装計画として再構築したもの。

plan_hit_rate.md §C との対応:

| plan_hit_rate §C | 本 Plan |
|---|---|
| C-1 計算式 | §3 実装範囲で引き継ぎ |
| C-2 NULL 時の重み再正規化 | §3 実装範囲で引き継ぎ |
| C-3 combo_range_backtest への行追加方式 | §3 実装範囲で引き継ぎ |
| C-4 重み固定 0.6/0.3/0.1 推奨 | §1.4 で確定 |

### 1.3 補遺3 整合性（HTML 表示なし方針）

`90_決定ログ.md` 補遺3 で確定した表示仕様:

- **出力はすべての要素で min〜max のみ。avg は出さない。**
- 数: `min匹 〜 max匹`、型: `min cm 〜 max cm`、重: `min kg 〜 max kg`

**composite_hit_rate は内部評価メトリックであり、ユーザーへの表示対象ではない。**

この方針は Phase C 全体を通じて一貫して維持する。composite を HTML に出力する実装は禁止。理由:

1. composite は「cnt/size/kg が同時に範囲内に入った割合」という内部統計であり、釣り人にとって直接的な意味を持たない
2. 補遺3 の「min〜max のみ」方針は「複合スコアを別指標として表示しない」趣旨とも整合する
3. crawler.py forecast HTML での composite 参照ルートを作ると、補遺3 違反リスクが生じる（§8 リスク 3 参照）

### 1.4 重み 0.6/0.3/0.1 の確定根拠

plan_hit_rate §C-4 の推奨根拠:

- kg の NULL 率 70.2% のため、kg の重み最適化は困難（有効サンプルが少なすぎる）
- 重みを変動させると重み選択バイアスが生じる
- 0.6/0.3/0.1 は「cnt を最重視し、size を中程度、kg を補助」という事業方針と整合する
- kg NULL 率が高い実態では、実運用の大半が cnt+size 構成（再正規化後 0.667/0.333）になる

---

## §2 ユーザー判断（確定値）

### 判断 1: 主指標の方向 → **promise_break 方向（低いほど良い）**

確定値: composite_promise_break として記録（既存 cnt/size/kg と方向統一）。

実装影響:
- 主指標カラム: `promise_break_rate` に composite 用の値を格納（既存スキーマ流用）
- 数式: `1 - 加重平均(is_*_in_range)` または直接 `加重平均(is_*_below_lo)`
- クエリ時に他 metric と単一クエリで比較可能

### 判断 2: cnt のみコンボの扱い → **出す + component_count 列追加**

確定値: cnt のみコンボ（size/kg ともに NULL）にも composite 値を記録。ただし「何個の指標で計算したか」を `component_count` 列で明示する。

実装影響:
- combo_range_backtest の CREATE TABLE に `component_count INTEGER` 列を追加（§3.2 詳細）
- composite 行のみ `component_count` を 1/2/3 で記録:
  - 1: cnt のみ
  - 2: cnt + size または cnt + kg
  - 3: 全揃い（cnt + size + kg）
- 既存 cnt/size/kg 行は `component_count = NULL`（8 要素タプルの末尾に None を追加 → §3.4 詳細）
- コンボ数: 最大 290（cnt 有効全コンボ）
- クエリ時に `WHERE component_count >= 2` で「複数指標統合」のみ抽出可能（domain 推奨）

### 判断 3: 副指標の保持範囲 → **promise_break + coverage + over_expect + winkler（4 軸全保存）**

確定値: cnt/size/kg と同じ 4 指標構造を保つ。クエリ用途を広く保ち、後の解析で柔軟性を確保。

実装影響:
- composite 行も既存スキーマの 4 指標列（promise_break_rate / coverage / over_expect_rate / winkler）を全て保存
- bowzu_rate は composite では NULL（cnt のみ計算可能・統合する意味薄い）
- 計算方法:
  - composite_promise_break = 行レベル加重平均（is_below_lo の重み付き平均を全行で平均）
  - composite_coverage = 行レベル加重平均（is_in_range の重み付き平均を全行で平均）
  - composite_over_expect = 行レベル加重平均（is_above_hi の重み付き平均を全行で平均）
  - composite_winkler = NULL（v1.2 では未実装・§8 リスク 7 参照）

### 判断 4: 採用/撤回判定方針 → **エラーさえ出なければ採用**

確定値: Phase C は新規行追加のみで既存 cnt/size/kg を壊さない。実装エラーなしで成功基準（§4）クリアなら採用確定。

実装影響:
- §4 の成功基準で十分（撤回基準 X pt は不要・新規メトリック追加のため）
- ロールバック計画は不要（既存値非破壊なので戻す必要がない）
- 全コンボ再実行後の判定は「成功基準全クリア → engineer コミット」の単純フロー

### 技術確定事項（ユーザー判断不要・既存実装との整合性ルール）

**cnt 評価軸: actual_avg ベース（既存単体 cnt 評価と統一）**

実コード L3636-3658 の cnt ループは `act_avg = d["cnt_avg"][1]`（actual_avg）で評価している。composite の cnt 成分も同じ軸を使うことで、cnt 単体評価と整合する。

- cnt 成分の `is_cnt_below = 1.0 if act_avg < pred_lo else 0.0`（actual_avg ベース）
- cnt 成分の `is_cnt_above = 1.0 if act_avg > pred_hi else 0.0`（actual_avg ベース）
- cnt 成分の `is_cnt_in = 1.0 if pred_lo <= act_avg <= pred_hi else 0.0`（actual_avg ベース）

**size/kg 評価軸: actual_min/max ベース（Phase B-α'/β-4 と統一）**

size（L3704-3713）/ kg（L3762-3767）は actual_min/max ベースで評価している。composite の size/kg 成分も同じ軸を維持する。

---

## §3 実装範囲

### 3.1 対象ファイル

| ファイル | 変更種別 | 変更内容 |
|---|---|---|
| `analysis/V2/methods/combo_deep_dive.py` | 追記・修正 | composite 計算ブロック追加（L3779 kg ループ末尾の直後）+ CREATE TABLE 列追加 + save_range_backtest unpack 変更 |
| `analysis/V2/results/analysis.sqlite` | 行追加・テーブル再作成 | combo_range_backtest に metric='composite' 行追加・component_count INTEGER 列追加（needs_recreate で DROP→CREATE） |

**変更しないファイル**:
- `analysis/V2/methods/predict_count.py`: composite は内部評価のみ
- `crawler.py`: forecast HTML への composite 表示は補遺3 整合性から禁止
- `docs/*.html`: 同上

### 3.2 CREATE TABLE 変更と needs_recreate 拡張

**ALTER TABLE は使わない。** CREATE TABLE への列追加 + `needs_recreate` 判定拡張の方式を採用する。

理由: ALTER TABLE は実行タイミング（どの関数内で実行するか）が曖昧になりやすく、既存の `needs_recreate` ロジック（DROP→CREATE）との併存が複雑になる。CREATE TABLE 列追加 + `needs_recreate` 拡張で一度の DROP→CREATE により全行が新スキーマになる。

**変更箇所 1: `save_range_backtest()` 内の CREATE TABLE 文（L4565-4580 相当）**

```python
# 変更前 (PRIMARY KEY 行の後・winkler と n の間に component_count がない)
conn.execute("""
    CREATE TABLE IF NOT EXISTS combo_range_backtest (
        fish               TEXT,
        ship               TEXT,
        metric             TEXT,
        horizon            INTEGER,
        promise_break_rate REAL,
        over_expect_rate   REAL,
        coverage           REAL,
        bowzu_rate         REAL,
        winkler            REAL,
        n                  INTEGER,
        updated_at         TEXT,
        PRIMARY KEY (fish, ship, metric, horizon)
    )
""")

# 変更後（component_count INTEGER 列を updated_at の直前に追加）
conn.execute("""
    CREATE TABLE IF NOT EXISTS combo_range_backtest (
        fish               TEXT,
        ship               TEXT,
        metric             TEXT,     -- "cnt" / "size" / "kg" / "composite"
        horizon            INTEGER,
        promise_break_rate REAL,
        over_expect_rate   REAL,
        coverage           REAL,
        bowzu_rate         REAL,
        winkler            REAL,
        n                  INTEGER,
        component_count    INTEGER,  -- composite のみ 1/2/3。cnt/size/kg は NULL
        updated_at         TEXT,
        PRIMARY KEY (fish, ship, metric, horizon)
    )
""")
```

**変更箇所 2: `save_range_backtest()` 内の `needs_recreate` 判定（L4555-4560 相当）**

```python
# 変更前
needs_recreate = (
    existing_cols and (
        "metric" not in existing_cols
        or "max_over_rate" in existing_cols
    )
)

# 変更後（component_count がなければ再作成）
needs_recreate = (
    existing_cols and (
        "metric" not in existing_cols
        or "max_over_rate" in existing_cols
        or "component_count" not in existing_cols   # ← 追加
    )
)
```

この変更により、既存の analysis.sqlite には `component_count` がないため DROP→CREATE が自動実行される。既存 cnt/size/kg 行は再実行で復元される（run_full_deepdive.py 実行時に全コンボが再計算される前提）。

### 3.3 composite 計算ブロック（実コードへの挿入位置と実装）

**挿入位置**: L3779 の `range_bt_data.append(("kg", ...))` の直後・L3781（star_backtest セクション）の前。

実コードの確認済みキー構造（`_range_by_key[H][_rk]`）:
- `d["cnt_min"]` → `(pred_lo, act_min)` タプル（cnt_min モデルの pred と actual_cnt_min）
- `d["cnt_max"]` → `(pred_hi, act_max)` タプル（cnt_max モデルの pred と actual_cnt_max は未使用）
- `d["cnt_avg"]` → `(pred_avg, act_avg)` タプル（cnt_avg モデルの pred と actual_avg）
- `d["size_avg"]` → `(pred_c, act_avg_size)` タプル（size_avg モデルの pred と actual_size_avg）
- `d["_size_min_act"]` → スカラー（actual_size_min）
- `d["_size_max_act"]` → スカラー（actual_size_max）
- `d["_decade_no"]` → スカラー（旬番号）
- `d["kg_avg"]` → `(pred_c, act_avg_kg)` タプル
- `d["_kg_min_act"]` → スカラー（actual_kg_min）
- `d["_kg_max_act"]` → スカラー（actual_kg_max）

size/kg の pred_lo/pred_hi は `_range_by_key` に格納されていない（各メトリックループ内でローカル計算）。composite ブロック内で再計算する。`_bt_decadal_size_ratio` / `_bt_decadal_kg_ratio` は `deep_dive()` 関数スコープ内の変数として利用可能。

```python
    # ── composite メトリック（Phase C: 加重平均 hit_rate）─────────────────────────
    # 設計根拠: plan_C_2026-05-08.md §3 / plan_hit_rate_2026-05-08.md §C-1〜C-4
    # 補遺3 整合: composite は内部評価のみ。HTML 表示禁止（§8 リスク 3 参照）。
    #
    # 重み（plan_C §1.4 確定値）:
    #   W_CNT = 0.6 / W_SIZE = 0.3 / W_KG = 0.1
    #
    # NULL 時の重み再正規化:
    #   有効メトリックの重みのみで正規化（0 除算ガード必須）
    #   cnt は必須（cnt なし → composite 計算不可・行スキップ）
    #
    # 評価軸の統一（§2 技術確定事項）:
    #   cnt  → actual_avg ベース（実コード L3636-3658 の cnt ループと統一）
    #   size → actual_min/max ベース（Phase B-α' L3704-3713 と統一）
    #   kg   → actual_min/max ベース（Phase B-β-4 L3762-3767 と統一）
    #
    # フラグの排他性保証（数学的保証）:
    #   cnt:  is_cnt_below + is_cnt_above + is_cnt_in <= 1
    #         （actual_avg < pred_lo / > pred_hi / 範囲内 は排他的）
    #   size: is_size_below + is_size_above + is_size_in <= 1
    #         （actual_min < pred_lo / actual_max > pred_hi / 完全包含 は排他的）
    #   kg:   同様
    #   ※ size/kg は「一方の端だけ外れる」場合 below=0, above=0, in=0 も有り得る
    #      （actual_min >= pred_lo かつ actual_max > pred_hi = above=1, in=0）
    #
    # 行レベル集計採用の根拠:
    #   各釣行（行）単位で cnt/size/kg の同時達成度を加重平均し、全行で再平均する方式。
    #   テーブルレベル集計（metric ごとの promise_break_rate を加重平均）では
    #   「cnt が良くて size が悪いコンボ」の悪化を過小評価するリスクがある。
    #   行レベルは「全指標が同時に範囲内に入った率」という統合評価の意味を保つ。
    #   具体例: cnt が良好(0%)・size が悪い(100%) の 1 行コンボの場合、
    #     テーブルレベル(0.667×0 + 0.333×1 = 0.333)よりも
    #     行レベル(W_CNT×0 + W_SIZE×1)/w_sum = 0.333 で一致するが、
    #     複数行が混在するコンボでは行順序の影響を受けないため行レベルが適切。
    W_CNT, W_SIZE, W_KG = 0.6, 0.3, 0.1

    for H in HORIZONS:
        composite_rows = []
        for _rk, d in _range_by_key[H].items():

            # ── cnt（必須）: actual_avg ベース ──────────────────────────────────
            # L3636-3658 の cnt ループと評価軸を統一
            if "cnt_min" not in d or "cnt_max" not in d or "cnt_avg" not in d:
                continue
            pred_lo_cnt, _ = d["cnt_min"]   # cnt_min モデルの pred 値
            pred_hi_cnt, _ = d["cnt_max"]   # cnt_max モデルの pred 値
            _,    act_avg  = d["cnt_avg"]   # actual_avg（cnt 評価軸）
            if pred_lo_cnt is None or pred_hi_cnt is None or act_avg is None:
                continue
            # フラグは排他的（is_below + is_above + is_in <= 1 が数学的に保証される）
            is_cnt_below = 1.0 if act_avg < pred_lo_cnt else 0.0
            is_cnt_above = 1.0 if act_avg > pred_hi_cnt else 0.0
            is_cnt_in    = 1.0 if pred_lo_cnt <= act_avg <= pred_hi_cnt else 0.0

            # ── size（任意）: actual_min/max ベース、pred は再計算 ───────────────
            # Phase B-α' L3677-3726 の size ループと評価軸を統一
            # pred_lo/pred_hi は _range_by_key に未格納のため composite ブロックで再計算
            is_size_below = is_size_above = is_size_in = None
            if "size_avg" in d:
                pred_c_sz, _ = d["size_avg"]   # size_avg モデルの pred 値（点予測）
                act_sz_min   = d.get("_size_min_act")
                act_sz_max   = d.get("_size_max_act")
                dec_no       = d.get("_decade_no")
                if (pred_c_sz is not None
                        and act_sz_min is not None and act_sz_max is not None):
                    _rmin_sz, _rmax_sz = _bt_decadal_size_ratio.get(
                        dec_no, (_global_ratio_min_p20, _global_ratio_max_p80)
                    )
                    pred_lo_sz = pred_c_sz * _rmin_sz
                    pred_hi_sz = pred_c_sz * _rmax_sz
                    # フラグは排他的（size は actual_min/max ベース）
                    is_size_below = 1.0 if act_sz_min < pred_lo_sz else 0.0
                    is_size_above = 1.0 if act_sz_max > pred_hi_sz else 0.0
                    is_size_in    = (1.0 if (act_sz_min >= pred_lo_sz
                                             and act_sz_max <= pred_hi_sz) else 0.0)

            # ── kg（任意）: actual_min/max ベース、pred は再計算 ─────────────────
            # Phase B-β-4 L3737-3779 の kg ループと評価軸を統一
            is_kg_below = is_kg_above = is_kg_in = None
            if "kg_avg" in d:
                pred_c_kg, _ = d["kg_avg"]   # kg_avg モデルの pred 値（点予測）
                act_kg_min   = d.get("_kg_min_act")
                act_kg_max   = d.get("_kg_max_act")
                dec_no_kg    = d.get("_decade_no")  # _decade_no は size と共通キー
                if (pred_c_kg is not None
                        and act_kg_min is not None and act_kg_max is not None):
                    _rmin_kg, _rmax_kg = _bt_decadal_kg_ratio.get(
                        dec_no_kg, (_global_kg_ratio_min_p20, _global_kg_ratio_max_p80)
                    )
                    pred_lo_kg = pred_c_kg * _rmin_kg
                    pred_hi_kg = pred_c_kg * _rmax_kg
                    # フラグは排他的（kg は actual_min/max ベース）
                    is_kg_below = 1.0 if act_kg_min < pred_lo_kg else 0.0
                    is_kg_above = 1.0 if act_kg_max > pred_hi_kg else 0.0
                    is_kg_in    = (1.0 if (act_kg_min >= pred_lo_kg
                                           and act_kg_max <= pred_hi_kg) else 0.0)

            # ── 重み再正規化 ─────────────────────────────────────────────────────
            w_sum = W_CNT
            component_count = 1   # cnt は必須なので 1 から始まる
            if is_size_in is not None:
                w_sum += W_SIZE
                component_count += 1
            if is_kg_in is not None:
                w_sum += W_KG
                component_count += 1

            if w_sum <= 0:
                continue   # 防御的ガード（理論上 w_sum >= 0.6 のはず）

            # ── 4 指標を行レベル加重平均（正規化済み）───────────────────────────
            below_w = W_CNT * is_cnt_below
            above_w = W_CNT * is_cnt_above
            in_w    = W_CNT * is_cnt_in
            if is_size_in is not None:
                below_w += W_SIZE * is_size_below
                above_w += W_SIZE * is_size_above
                in_w    += W_SIZE * is_size_in
            if is_kg_in is not None:
                below_w += W_KG * is_kg_below
                above_w += W_KG * is_kg_above
                in_w    += W_KG * is_kg_in

            composite_rows.append({
                "below": below_w / w_sum,
                "above": above_w / w_sum,
                "in":    in_w    / w_sum,
                "comp":  component_count,
            })

        n_comp = len(composite_rows)
        if n_comp < MIN_N_RANGE:
            continue

        # ── 4 指標の集計（行レベル平均）─────────────────────────────────────────
        comp_pb  = sum(r["below"] for r in composite_rows) / n_comp
        comp_oe  = sum(r["above"] for r in composite_rows) / n_comp
        comp_cov = sum(r["in"]    for r in composite_rows) / n_comp
        # winkler は cnt/size/kg で単位が異なるため加重平均は意味をなさない → NULL
        comp_winkler = None

        # component_count 代表値: 平均値の四捨五入（最頻値の近似）
        # 注意: 同一コンボ内で行ごとに component_count が異なる場合（kg が一部行のみ存在等）、
        # 代表値は行間の分布を潰す。層別解析が必要な場合は §8 リスク 8 参照。
        avg_comp = sum(r["comp"] for r in composite_rows) / n_comp
        comp_count_repr = round(avg_comp)

        range_bt_data.append((
            "composite", H, comp_pb, comp_oe, comp_cov, None, comp_winkler, n_comp,
            comp_count_repr   # 9 要素目: component_count（save_range_backtest で受け取る）
        ))
```

**winkler の取り扱い**: cnt/size/kg の winkler は単位が異なる（cnt は匹ベース区間幅、size は cm ベース、kg は kg ベース）。単純加重平均は単位混在で意味をなさないため NULL とする。後の Phase で行レベルスコアを無次元化してから加重平均する実装は可能（リスク 7 参照）。

### 3.4 save_range_backtest() の変更

現在の unpack: `for metric, H, pbr, oer, cov, bzr, wkl, n in range_bt_data`（8 要素）。

**§3.3 で composite を 9 要素タプルとして `range_bt_data.append()` するため、cnt/size/kg の 3 箇所も 9 要素に統一しなければ ValueError が発生する。**

**変更箇所 1: cnt/size/kg の range_bt_data.append（3 箇所）に末尾 None を追加**

```python
# L3657-3658（cnt ループ末尾）
# 変更前
range_bt_data.append(("cnt", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_r))
# 変更後（末尾に None 追加・component_count = NULL）
range_bt_data.append(("cnt", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_r, None))

# L3725-3726（size ループ末尾）
# 変更前
range_bt_data.append(("size", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_valid))
# 変更後
range_bt_data.append(("size", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_valid, None))

# L3778-3779（kg ループ末尾）
# 変更前
range_bt_data.append(("kg", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_valid))
# 変更後
range_bt_data.append(("kg", H, promise_break_rate, over_expect_rate,
                       coverage, bowzu_rate, winkler, n_valid, None))
```

**変更箇所 2: save_range_backtest() の unpack と INSERT 文**

```python
# 変更前（L4582-4591）
rows = [
    (fish, ship, metric, H, pbr, oer, cov, bzr, wkl, n, now)
    for metric, H, pbr, oer, cov, bzr, wkl, n in range_bt_data
]
conn.executemany("""
    INSERT OR REPLACE INTO combo_range_backtest
    (fish, ship, metric, horizon, promise_break_rate, over_expect_rate, coverage,
     bowzu_rate, winkler, n, updated_at)
    VALUES (?,?,?,?,?,?,?,?,?,?,?)
""", rows)

# 変更後（9 要素 unpack + component_count 列追加）
rows = [
    (fish, ship, metric, H, pbr, oer, cov, bzr, wkl, n, comp_count, now)
    for metric, H, pbr, oer, cov, bzr, wkl, n, comp_count in range_bt_data
]
conn.executemany("""
    INSERT OR REPLACE INTO combo_range_backtest
    (fish, ship, metric, horizon, promise_break_rate, over_expect_rate, coverage,
     bowzu_rate, winkler, n, component_count, updated_at)
    VALUES (?,?,?,?,?,?,?,?,?,?,?,?)
""", rows)
```

---

## §4 成功基準（撤回基準の代わり）

判断 4 確定: Phase C は既存値非破壊・新規行追加のみ。撤回基準は不要。

| # | 基準 | 確認方法 |
|---|---|---|
| 1 | 実装エラーがない（例外・AttributeError 等なし）| 1 コンボ動作確認（§5）|
| 2 | composite 値が 0〜1 の範囲に収まる | 1 コンボ動作確認で出力値を目視確認 |
| 3 | 有効重み合計が常に正（w_sum > 0 ガード）| ゼロ除算が発生しないことを確認 |
| 4 | combo_range_backtest の cnt/size/kg 行数が変わらない | 全コンボ実行前後で行数を比較 |
| 5 | metric='composite' 行が新規追加されている | 全コンボ実行後に `SELECT COUNT(*) WHERE metric='composite'` |
| 6 | PRIMARY KEY 衝突エラーが 0 件 | 実行ログに `UNIQUE constraint failed` がないこと |
| 7 | component_count が 1/2/3 のみ（0/4 以上が出ない）| `SELECT DISTINCT component_count, COUNT(*) FROM combo_range_backtest WHERE metric='composite' GROUP BY component_count` |
| 8 | 既存 cnt/size/kg 行の component_count が NULL | `SELECT COUNT(*) WHERE metric IN ('cnt','size','kg') AND component_count IS NOT NULL` の結果が 0 |
| 9 | composite_coverage が 0.05 以上（著しく低くない）| 確認 SQL 1 の avg_cov を目視確認（domain 推奨・§8 リスク 9 参照）|

---

## §5 1コンボ動作確認手順

### 5.1 確認コンボ

1. マダイ × ちがさき丸（n=42）
2. マハタ × 幸丸（n=83）
3. cnt のみコンボ（size/kg ともに NULL のコンボを 1 件選定）→ component_count=1 の動作確認

### 5.2 動作確認手順

```bash
# ステップ 1: マダイ×ちがさき丸 1コンボ実行
python analysis/V2/methods/combo_deep_dive.py --fish マダイ --ship ちがさき丸
```

```python
# ステップ 2: combo_range_backtest の composite 行を確認
import sqlite3
conn = sqlite3.connect('analysis/V2/results/analysis.sqlite')
rows = conn.execute(
    "SELECT fish, ship, metric, horizon, promise_break_rate, "
    "over_expect_rate, coverage, winkler, n, component_count "
    "FROM combo_range_backtest WHERE fish='マダイ' AND ship='ちがさき丸' "
    "ORDER BY metric, horizon"
).fetchall()
with open('tmp_composite_check.txt', 'w', encoding='utf-8') as f:
    for r in rows:
        f.write(str(r) + '\n')
conn.close()
```

```bash
type tmp_composite_check.txt && del tmp_composite_check.txt
```

**合格条件（マダイ×ちがさき丸）**:
- `metric='composite'` の行が存在する（horizon ごとに 1 行）
- `promise_break_rate` / `over_expect_rate` / `coverage` が 0〜1 の範囲内
- `component_count` が 1/2/3 のいずれか
- `metric='cnt'` / `metric='size'` / `metric='kg'` の行数と数値が変わっていない
- `metric='cnt'` / `metric='size'` / `metric='kg'` の `component_count` が NULL

**行レベル vs テーブルレベルの検証（stat-reviewer 指摘）**:

```python
# ステップ 3: マダイ×ちがさき丸 H=0 の行レベル composite_pb と
#             既存 cnt_pb/size_pb の加重平均を比較して妥当性確認
import sqlite3
conn = sqlite3.connect('analysis/V2/results/analysis.sqlite')
rows = conn.execute(
    "SELECT metric, horizon, promise_break_rate, n, component_count "
    "FROM combo_range_backtest WHERE fish='マダイ' AND ship='ちがさき丸' "
    "AND horizon = 0 ORDER BY metric"
).fetchall()
with open('tmp_composite_level_check.txt', 'w', encoding='utf-8') as f:
    f.write("metric, horizon, promise_break_rate, n, component_count\n")
    for r in rows:
        f.write(str(r) + '\n')
    # 参考: cnt_pb×0.667 + size_pb×0.333 のテーブルレベル近似値を計算
    d = {r[0]: r for r in rows}
    cnt_pb  = d.get('cnt',  [None]*3)[2] or 0.0
    size_pb = d.get('size', [None]*3)[2]
    if size_pb is not None:
        table_level_approx = cnt_pb * (0.6/0.9) + size_pb * (0.3/0.9)
        f.write(f"\nテーブルレベル近似(cnt+size 再正規化): {table_level_approx:.4f}\n")
        f.write(f"行レベル composite_pb (実装値): {d.get('composite', [None]*3)[2]}\n")
        f.write("※ 多行コンボでは値が異なることが期待値（行順序の違い）\n")
conn.close()
```

```bash
type tmp_composite_level_check.txt && del tmp_composite_level_check.txt
```

### 5.3 cnt のみコンボの動作確認

```sql
-- cnt のみコンボの特定（size 行が存在しない）
SELECT DISTINCT a.fish, a.ship FROM combo_range_backtest a
WHERE a.metric = 'cnt' AND a.horizon = 0
AND NOT EXISTS (
  SELECT 1 FROM combo_range_backtest b
  WHERE b.fish = a.fish AND b.ship = a.ship AND b.metric = 'size'
)
LIMIT 5;
```

このコンボで 1 コンボ実行し、`metric='composite'` 行の `component_count = 1` であることを確認。

### 5.4 全揃いコンボの確認

```sql
-- size と kg 両方が記録されているコンボ（component_count=3 期待）
SELECT a.fish, a.ship FROM combo_range_backtest a
WHERE a.metric = 'kg' AND a.horizon = 0 AND a.n >= 30
LIMIT 5;
```

このコンボで `component_count = 3` を確認。

---

## §6 全コンボ再実行コスト

実行コマンド:
```bash
python analysis/V2/methods/run_full_deepdive.py --workers 4 --reset-best
```

所要時間見積もり: 約 27〜30 分
- Phase B-β-4（kg）の実績: 26分59秒
- composite 追加による増加分は軽微（行レベル集計の追加のみ）

タイミング推奨: 12:00 JST 以前開始（GitHub Actions 16:30 JST 衝突回避）。

---

## §7 4並列レビュー観点（v1.2 更新版）

v1.2 で対応した CRITICAL/MAJOR/MINOR 指摘の確認を含む。

### analyst（実装妥当性）

確認観点:
1. `_range_by_key[H][_rk]` のキー確認: `d["cnt_min"]` / `d["cnt_max"]` / `d["cnt_avg"]` がタプルであること（§3.3 コードはタプルアンパックで取得）
2. 重み再正規化ロジック（w_sum > 0 ガード）が実装されているか
3. component_count 計算が正しいか（cnt 必須=1 から始まり size/kg で +1 ずつ）
4. composite 計算ブロックが **L3779 の直後・L3781（star_backtest）の前**に配置されているか（実行順序）
5. cnt/size/kg の range_bt_data.append が全 3 箇所で **9 要素**（末尾 None 追加）になっているか（§3.4 変更箇所 1）
6. save_range_backtest() の unpack が **9 要素**（`metric, H, pbr, oer, cov, bzr, wkl, n, comp_count`）になっているか（§3.4 変更箇所 2）
7. CREATE TABLE に `component_count INTEGER` 列が追加されているか（§3.2 変更箇所 1）
8. `needs_recreate` 判定に `"component_count" not in existing_cols` が追加されているか（§3.2 変更箇所 2）
9. size/kg の pred_lo/pred_hi が composite ブロック内で `_bt_decadal_size_ratio` / `_bt_decadal_kg_ratio` を使って再計算されているか（§3.3 コード参照）
10. INSERT OR REPLACE が PRIMARY KEY (fish, ship, metric, horizon) で動作することの確認
11. winkler の NULL 取り扱い（v1.2 でも NULL・理由は §3.3 コメント）

### stat-reviewer（統計的妥当性）

確認観点:
1. 重み 0.6/0.3/0.1 の統計的妥当性: kg NULL 率 70.2% で「cnt+size 構成が大半」になることは許容できるか
2. 行レベル集計の根拠が §3.3 コードコメントに明記されているか（v1.2 で追加・「テーブルレベル比較例」も確認）
3. NULL 重み再正規化後の composite が 0〜1 に収まる数学的保証（`w_sum > 0` ガード確認）
4. フラグの排他性コメント（`is_below + is_above + is_in <= 1` の保証）が §3.3 コメントに明記されているか（v1.2 で追加）
5. component_count 代表値の統計的限界注記（§3.3 コメントおよび §8 リスク 8）が記載されているか
6. §5.2 ステップ 3 の「行レベル vs テーブルレベル」比較 SQL が動作するか

### data-reviewer（join粒度・欠損値・集計単位）

確認観点:
1. `_range_by_key[H][_rk]` のキー命名が §3.3 に明記されており、KeyError リスクが解消されているか
2. `metric='composite'` の行が既存の cnt/size/kg と同じ `(fish, ship, horizon)` から計算されているか
3. 全コンボ実行前後で `metric != 'composite'` の行数・数値が変わらないこと（成功基準 4・8）
4. PRIMARY KEY 衝突: 同一 `(fish, ship, 'composite', horizon)` が 2 回以上 INSERT されないこと
5. needs_recreate による DROP→CREATE 後、全コンボ再実行で cnt/size/kg 行が正しく復元されるか
6. cnt のみコンボ（component_count=1）の集計が他と区別されているか（成功基準 7・8）

### domain（主指標選択の現場感）

確認観点:
1. `is_cnt_in_range` の定義（actual_avg が [pred_lo, pred_hi] 内）は釣り人視点で「当たった」と感じる定義として適切か
2. component_count=1（cnt のみ）と component_count=3（全揃い）の composite を同じ列で比較するのは意味があるか（`WHERE component_count >= 2` での絞り込みを推奨として §2 に追記済み）
3. kg NULL 率 70.2% で composite が実質 cnt+size 構成になるが、釣り人にとって有用か
4. 成功基準 9: composite_coverage < 0.05 は要確認という注記が適切か

---

## §8 リスクと対策

### リスク 1: 既存 cnt/size/kg 行の値が壊れる

**症状**: composite 計算ブロックが `_range_by_key` を変更し、後続処理で欠損

**対策**: composite ブロックは `_range_by_key[H]` を読み取りのみで参照。reviewer が write 操作の不在を確認

**検証**: 成功基準 4・8（既存行数・数値・component_count NULL の確認）

### リスク 2: NULL 重み再正規化のバグ（除算 0・指標欠損）

**症状**: `w_sum = 0.0` で除算実行・ZeroDivisionError

**対策**: `if w_sum <= 0: continue` ガード実装。cnt が None の行は composite 対象外（理論上 w_sum >= 0.6）

**検証**: 1 コンボ動作確認で composite_val が 0〜1 に収まることを確認

### リスク 3: HTML 表示で composite を誤って表示してしまう（補遺3 違反）

**症状**: 実装者が composite を crawler.py または forecast HTML に追加してしまう

**対策**:
- 本 Plan §1.3 に「HTML 表示禁止」を明記
- engineer コミット前に `grep -n "composite" crawler.py` で参照不在確認

**補遺3 再確認**: composite はユーザー表示対象ではない。analysis.sqlite 内部評価のみ

### リスク 4: 重み固定の妥当性（kg NULL 率 71% での実質値）

**症状**: kg 有効コンボ少なく（103 コンボ）、composite 計算に kg 寄与少ない

**対策**: 設計上の意図は「kg 記録がある場合のみ加点」で正しい動作。実質 cnt+size 構成（0.667/0.333）になることは許容（plan_hit_rate §C-4 既知）

### リスク 5: `_range_by_key` キー名不一致（v1.2 で解消）

**v1.1 指摘（analyst CRITICAL 1）**: 存在しないキー（`_cnt_pred_lo` 等）を参照していた。

**v1.2 修正**: 実コードの実際のキー構造（§3.3 の確認済みキー一覧）を反映。タプルアンパックで正しく取得。

**検証**: 実装前に 1 コンボで `print(f'keys: {list(d.keys())}')` を実行してキー確認（CLAUDE.md ルール）

### リスク 6: component_count 列追加で既存クエリが影響を受ける

**症状**: 既存の `SELECT * FROM combo_range_backtest` クエリが新列を読み込み、後続処理で予期しない動作

**対策**: DROP→CREATE による再作成後、全コンボ再実行で全行が新スキーマで統一される。既存クエリは `SELECT *` でも新列を無視できる

**検証**: 全コンボ実行後に predict_count.py 等の caller が変わらず動作することを確認

### リスク 7: winkler を NULL にする判断の妥当性

**症状**: composite_winkler が常に NULL のため、後の解析で活用できない

**対策**: v1.2 では NULL 採用。理由: cnt の winkler は匹ベース区間幅、size は cm、kg は kg と単位が異なり加重平均は意味をなさない。無次元化（各メトリックの winkler を自身の scale で正規化）が必要。必要と判断した場合は Phase D で別実装。

### リスク 8: component_count 代表値の統計的限界（stat-reviewer MAJOR-2 指摘）

**症状**: 同一コンボ内で行ごとに component_count が異なる場合（kg が一部行のみ存在等）、代表値（平均の四捨五入）が行間変動を潰す。

**注意点**:
- component_count が行ごとに 1 と 3 が混在するコンボでは、代表値 2 が実態を反映しない
- 層別解析（component_count=1/2/3 で promise_break が系統的に異なるか）は別途確認が必要
- ランキング集計時は `WHERE component_count >= 2` を推奨（domain 推奨・§2 判断 2 に記載）

**検証**: §9 Phase 6 の確認 SQL 4 で `component_count` の分布ヒストグラム（DISTINCT + COUNT）を確認する

### リスク 9: composite_coverage 著しく低い（domain MINOR-1 指摘）

**症状**: composite_coverage が 0.05 未満のコンボが多い場合、「全指標が同時に範囲内」の基準が厳しすぎる可能性がある

**対策**: 成功基準 9 として追加（目視確認）。0.05 未満は「要確認」フラグ。閾値変更は Phase D で別途検討。

---

## §9 想定スケジュール

### Phase 1: 4並列再レビュー（本 Plan v1.2 提出後）

analyst / stat-reviewer / data-reviewer / domain の 4 agent 並列。各 reviewer は §7 の観点を渡す。

### Phase 2: 修正サイクル

Critical → 修正 → Phase 1 再実行。Major 以下 → 反映後に実装着手。

### Phase 3: 実装（analyst 担当）

実装挿入位置: **L3779 `range_bt_data.append(("kg", ...))` の直後・L3781（star_backtest セクション開始）の前**

1. `_range_by_key[H][_rk]` の格納キー確認（1 コンボで `print(f'keys: {list(d.keys())}')` 実行）
2. cnt/size/kg の `range_bt_data.append` 3 箇所に末尾 `None` を追加（§3.4 変更箇所 1）
3. `save_range_backtest()` の CREATE TABLE に `component_count INTEGER` 列を追加（§3.2 変更箇所 1）
4. `save_range_backtest()` の `needs_recreate` 判定に `"component_count" not in existing_cols` を追加（§3.2 変更箇所 2）
5. `save_range_backtest()` の unpack を 9 要素に変更・INSERT 文に `component_count` 列を追加（§3.4 変更箇所 2）
6. composite 計算ブロックを L3779 直後に挿入（§3.3 コードをそのまま使用）

### Phase 4: 1コンボ動作確認（§5 全手順）

- マダイ × ちがさき丸
- マハタ × 幸丸
- cnt のみコンボ（component_count=1）
- 全揃いコンボ（component_count=3）
- §5.2 ステップ 3: 行レベル vs テーブルレベル比較 SQL（stat-reviewer 検証）

### Phase 5: 全コンボ再実行（約 27〜30 分）

```bash
python analysis/V2/methods/run_full_deepdive.py --workers 4 --reset-best
```

### Phase 6: 成功基準チェック（§4 全 9 項目）

```sql
-- 確認 SQL 1: metric='composite' 行の追加確認(coverage 著しく低い場合は要確認)
SELECT metric, COUNT(*) as n, AVG(promise_break_rate) as avg_pb,
       AVG(coverage) as avg_cov, AVG(over_expect_rate) as avg_oe
FROM combo_range_backtest
WHERE horizon = 0
GROUP BY metric ORDER BY metric;

-- 確認 SQL 2: cnt/size/kg 行数が変わっていない(前後比較)
SELECT metric, COUNT(*) as n
FROM combo_range_backtest
WHERE horizon = 0 AND metric IN ('cnt', 'size', 'kg')
GROUP BY metric;

-- 確認 SQL 3: composite 値の範囲確認
SELECT MIN(promise_break_rate), MAX(promise_break_rate),
       MIN(coverage), MAX(coverage)
FROM combo_range_backtest
WHERE metric = 'composite';
-- 期待: すべて 0.0〜1.0 の範囲

-- 確認 SQL 4: component_count 分布ヒストグラム(stat-reviewer 指摘・代表値の妥当性確認)
SELECT component_count, COUNT(*) as n
FROM combo_range_backtest
WHERE metric = 'composite' AND horizon = 0
GROUP BY component_count
ORDER BY component_count;
-- 期待: 1/2/3 のみ・他の値なし・分布の偏りを目視確認

-- 確認 SQL 5: 既存 cnt/size/kg 行の component_count NULL 確認
SELECT COUNT(*) FROM combo_range_backtest
WHERE metric IN ('cnt', 'size', 'kg') AND component_count IS NOT NULL;
-- 期待: 0

-- 確認 SQL 6（domain 推奨）: ランキング用・component_count >= 2 絞り込み確認
SELECT fish, ship, promise_break_rate, coverage, component_count
FROM combo_range_backtest
WHERE metric = 'composite' AND horizon = 0 AND component_count >= 2
ORDER BY promise_break_rate
LIMIT 10;
```

### Phase 7: engineer コミット

成功基準全クリアを確認してから engineer がコミット。コミット対象:
- `analysis/V2/methods/combo_deep_dive.py`
- `analysis/V2/analysis-improvement/plan_C_2026-05-08.md`（本 Plan）
- `analysis/V2/analysis-improvement/90_決定ログ.md`（補遺 10: composite 実装完了）

**コミット前確認**:
```bash
# composite が crawler.py に混入していないことを確認（補遺3 整合）
grep -n "composite" crawler.py
# 結果が 0 行であることを確認
```

---

## 参照

- `analysis/V2/analysis-improvement/plan_hit_rate_2026-05-08.md` §C（骨格の継承元）
- `analysis/V2/analysis-improvement/plan_size_2026-05-08.md`（Phase B-α' 実装パターン）
- `analysis/V2/analysis-improvement/plan_kg_2026-05-08.md` v2（Phase B-β-4 実装パターン）
- `analysis/V2/analysis-improvement/90_決定ログ.md` 補遺3/8/9（表示方針・size/kg 採用確定）
- `CLAUDE.md` マネタイズ方針（avg は出さない・min〜max のみ）
- `analysis/V2/methods/combo_deep_dive.py` L3445-3459（_range_by_key 格納）/ L3636-3658（cnt ループ）/ L3675-3726（size ループ）/ L3737-3779（kg ループ）/ L4537-4593（save_range_backtest）

---

## v1 → v1.1 → v1.2 変更履歴

### v1 → v1.1

| 確定事項 | 変更内容 |
|---|---|
| 判断 1 確定 | 主指標 = promise_break 方向（低いほど良い）。§2 判断 1 で「確定値」として記述 |
| 判断 2 確定 | cnt のみコンボも記録 + component_count 列追加。§3.2 ALTER TABLE 追加・§3.3 component_count 計算ロジック・§3.4 INSERT 拡張 |
| 判断 3 確定 | 4 軸全保存（promise_break + coverage + over_expect + winkler）。§3.3 で 4 指標の計算ロジック追加・winkler は v1.1 では NULL（リスク 7 で記述） |
| 判断 4 確定 | 成功基準のみ・撤回基準なし。§4 で 8 項目に拡張（component_count 関連の基準 7・8 追加）|

### v1.1 → v1.2（4並列レビュー指摘対応）

| 指摘分類 | 指摘内容 | 対応箇所 | 対応内容 |
|---|---|---|---|
| CRITICAL 1 | `_range_by_key` キー名不一致（存在しないキー参照）| §3.3 全面書き直し | 実コードの確認済みキー一覧明記・タプルアンパックで正しく取得・size/kg の pred_lo/hi を composite ブロック内で再計算 |
| CRITICAL 2 | cnt 評価軸の定義不一致（actual_min vs actual_avg）| §3.3・§2 技術確定事項追加 | cnt は actual_avg ベース（L3636-3658 と統一）・size/kg は actual_min/max ベース維持。技術確定事項として §2 に明記 |
| CRITICAL 3 + MAJOR(観点 5) | range_bt_data タプル混在（8 vs 9 要素）/ ALTER TABLE 実行場所未明示 | §3.2 全面書き直し・§3.4 変更箇所 1 追加 | ALTER TABLE 廃止 → CREATE TABLE 列追加 + needs_recreate 拡張方式に統一。cnt/size/kg append 3 箇所に末尾 None 追加（9 要素化）。save_range_backtest unpack を 9 要素に変更 |
| MAJOR-1（stat） | 行レベル集計の根拠未明記 | §3.3 コードコメント・§5.2 ステップ 3 追加 | 行レベル採用の根拠をコードコメントに追記。行レベル vs テーブルレベル比較 SQL を §5.2 ステップ 3 として追加 |
| MAJOR-2（stat） | component_count 代表値の統計的限界未明記 | §3.3 コードコメント・§8 リスク 8 追加 | コードコメントに限界注記追加。リスク 8 として「層別解析の妥当性は別途確認必要」「分布ヒストグラム確認」を追加。§9 Phase 6 確認 SQL 4 に GROUP BY 分布確認を追加 |
| MINOR-1（stat） | 排他性確認コメント未明記 | §3.3 コードコメント | `is_below + is_above + is_in <= 1` の数学的保証をコメントに明示 |
| MINOR-1（domain） | coverage 低下時の注記未記載 | §4 成功基準 9・§8 リスク 9 追加 | composite_coverage < 0.05 は要確認として成功基準 9 に追加。リスク 9 として対策明記 |
| MINOR-2（domain） | ランキング集計時の component_count >= 2 推奨未記載 | §2 判断 2・§9 Phase 6 確認 SQL 6 追加 | 「WHERE component_count >= 2 で複数指標統合のみ抽出可能」を判断 2 に記載。確認 SQL 6 を追加 |
| MINOR（analyst 観点 4） | composite 挿入位置の行番号未明示 | §3.3・§9 Phase 3 | 「L3779 の直後・L3781（star_backtest）の前」を §3.3 挿入位置に明記。§9 Phase 3 にも再掲 |

---

*作成: pm / 2026-05-09（v1）*
*ユーザー判断 4 点反映: pm / 2026-05-09（v1.1）*
*4並列レビュー CRITICAL3+MAJOR3+MINOR3 対応: pm / 2026-05-09（v1.2）*
*ステータス: v1.2 完成・4並列再レビュー待ち*
*次のステップ: 4並列再レビュー → 合格後 analyst 実装 → 1コンボ動作確認 → 全コンボ再実行（約 30 分）→ engineer コミット*
